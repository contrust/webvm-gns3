FROM --platform=i386 i386/debian:buster
ARG DEBIAN_FRONTEND=noninteractive
ENV LANG=en_US.UTF-8 \ 
	LANGUAGE=en_US \ 
	LC_ALL=en_US.UTF-8
RUN apt-get clean && apt-get update && apt-get -y upgrade
RUN apt-get -y install apt-utils gcc locales \
	python3 python3-pip vim unzip ruby nodejs \
	fakeroot dbus base whiptail hexedit \
	patch ucf \
	file luajit make dialog curl \
	less cowsay netcat-openbsd
RUN ["pip3", "install", "gns3-server"]
RUN useradd -m user && echo "user:password" | chpasswd
COPY --chown=user:user ./examples /home/user/examples
COPY --chown=user:user ./gns3 /home/user/gns3
RUN chmod -R +x  /home/user/examples/lua
# We set WORKDIR, as this gets extracted by Webvm to be used as the cwd. This is optional.
WORKDIR /home/user/
# We set env, as this gets extracted by Webvm. This is optional.
ENV HOME="/home/user" TERM="xterm" USER="user" SHELL="/bin/bash" EDITOR="vim" LANG="en_US.UTF-8" LC_ALL="C"
RUN echo 'root:password' | chpasswd
CMD ["gns3server", "--config", "gns3/configs/gns3_server.conf"]
